# 发布清单

本文档提供发布新版本的完整清单和步骤。

## 发布前准备

### 1. 版本规划

- [ ] 确定版本号（遵循语义化版本）
  - 主版本号：不兼容的 API 变更
  - 次版本号：向下兼容的功能新增
  - 修订号：向下兼容的问题修正

### 2. 代码质量检查

- [ ] 所有测试通过
  ```bash
  npm test
  npm run test:pbt
  ```

- [ ] 代码审查完成
- [ ] 没有已知的严重 bug
- [ ] 性能测试通过
- [ ] 安全审计完成

### 3. 文档更新

- [ ] 更新 `CHANGELOG.md`
  - 添加新版本条目
  - 列出所有变更
  - 标注破坏性变更

- [ ] 更新 `README.md`（如有必要）
- [ ] 更新 `BUILD.md`（如有构建变更）
- [ ] 更新 API 文档（如有 API 变更）

### 4. 版本号更新

- [ ] 更新 `package.json` 中的版本号
  ```json
  {
    "version": "0.2.0"
  }
  ```

- [ ] 更新 `src-tauri/Cargo.toml` 中的版本号
  ```toml
  [package]
  version = "0.2.0"
  ```

- [ ] 更新 `src-tauri/tauri.conf.json` 中的版本号
  ```json
  {
    "package": {
      "version": "0.2.0"
    }
  }
  ```

## 构建和测试

### 5. 本地构建测试

- [ ] 清理构建产物
  ```bash
  npm run build:clean
  ```

- [ ] 检查构建环境
  ```bash
  npm run build:check
  ```

- [ ] 执行完整构建
  ```bash
  npm run build:full
  ```

### 6. 平台特定构建

#### Windows

- [ ] 构建 Windows 版本
  ```bash
  npm run tauri:build:windows
  ```

- [ ] 测试 MSI 安装程序
  - 安装应用
  - 运行应用
  - 测试核心功能
  - 卸载应用

- [ ] 检查安装包大小（目标 < 5 MB）

#### Linux

- [ ] 构建 Linux 版本
  ```bash
  npm run tauri:build:linux
  ```

- [ ] 测试 DEB 包
  ```bash
  sudo dpkg -i realtime-voice-translation_*.deb
  ```

- [ ] 测试 AppImage
  ```bash
  chmod +x realtime-voice-translation_*.AppImage
  ./realtime-voice-translation_*.AppImage
  ```

- [ ] 测试核心功能
- [ ] 卸载测试

### 7. 功能验证

在每个平台上验证：

- [ ] 应用启动正常
- [ ] 会议模式工作正常
- [ ] 聊天模式工作正常
- [ ] 说话人识别功能
- [ ] 语言检测功能
- [ ] 语音识别功能
- [ ] 翻译功能
- [ ] 语音合成功能
- [ ] 网络监控功能
- [ ] 离线模式切换
- [ ] 会话历史记录
- [ ] 设置保存和加载
- [ ] 错误处理

### 8. 性能验证

- [ ] 启动时间 < 2 秒
- [ ] 端到端延迟 < 2 秒
- [ ] 内存使用 < 500 MB（峰值）
- [ ] CPU 使用合理
- [ ] 无内存泄漏（长时间运行测试）

## 发布流程

### 9. Git 操作

- [ ] 提交所有变更
  ```bash
  git add .
  git commit -m "chore: release v0.2.0"
  ```

- [ ] 创建版本标签
  ```bash
  git tag -a v0.2.0 -m "Release version 0.2.0"
  ```

- [ ] 推送到远程仓库
  ```bash
  git push origin main
  git push origin v0.2.0
  ```

### 10. GitHub Release

- [ ] 在 GitHub 上创建新 Release
  - 选择刚创建的标签
  - 填写 Release 标题：`实时语音翻译 v0.2.0`
  - 从 CHANGELOG.md 复制变更说明
  - 标注破坏性变更（如有）

- [ ] 上传构建产物
  - Windows MSI 安装程序
  - Linux DEB 包
  - Linux AppImage
  - 校验和文件（可选）

- [ ] 发布 Release

### 11. 自动更新配置（可选）

如果启用了自动更新：

- [ ] 生成签名文件
  ```bash
  # 签名文件在构建时自动生成
  ```

- [ ] 上传到更新服务器
  - 上传安装包
  - 上传签名文件
  - 更新版本清单 JSON

- [ ] 验证自动更新
  - 使用旧版本应用
  - 检查是否提示更新
  - 测试更新流程

## 发布后

### 12. 验证发布

- [ ] 从 GitHub Release 下载安装包
- [ ] 在干净的系统上安装
- [ ] 验证应用功能正常
- [ ] 检查自动更新（如启用）

### 13. 通知和文档

- [ ] 更新项目网站（如有）
- [ ] 发布更新公告
- [ ] 通知用户（邮件列表、社交媒体等）
- [ ] 更新文档网站

### 14. 监控

- [ ] 监控错误报告
- [ ] 收集用户反馈
- [ ] 跟踪性能指标
- [ ] 准备热修复（如需要）

## 回滚计划

如果发现严重问题：

### 15. 紧急回滚

- [ ] 从 GitHub Release 中移除有问题的版本
- [ ] 发布回滚公告
- [ ] 如启用自动更新，更新服务器配置
- [ ] 准备修复版本

## 版本发布时间表

### 建议的发布周期

- **主版本**: 每年 1-2 次
- **次版本**: 每季度 1-2 次
- **修订版本**: 根据需要随时发布

### 发布窗口

- **最佳时间**: 周二或周三
- **避免时间**: 周五、节假日前
- **时区考虑**: 选择大多数用户活跃的时间

## 紧急修复流程

对于严重 bug 的紧急修复：

1. [ ] 创建 hotfix 分支
   ```bash
   git checkout -b hotfix/v0.2.1
   ```

2. [ ] 修复问题并测试
3. [ ] 更新版本号（修订号 +1）
4. [ ] 快速构建和测试
5. [ ] 合并到 main 并打标签
6. [ ] 立即发布

## 检查清单模板

复制以下模板用于每次发布：

```markdown
## Release v0.X.X Checklist

### 准备
- [ ] 版本号确定
- [ ] 测试通过
- [ ] 文档更新
- [ ] 版本号更新

### 构建
- [ ] Windows 构建
- [ ] Linux 构建
- [ ] 功能验证
- [ ] 性能验证

### 发布
- [ ] Git 标签
- [ ] GitHub Release
- [ ] 上传产物
- [ ] 自动更新配置

### 后续
- [ ] 验证发布
- [ ] 通知用户
- [ ] 监控反馈
```

## 联系方式

如有发布相关问题，请联系：
- 技术负责人：[邮箱]
- 项目经理：[邮箱]
- 紧急联系：[电话]
